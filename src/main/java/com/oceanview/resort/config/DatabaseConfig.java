package com.oceanview.resort.config;

import org.springframework.context.annotation.Configuration;
import javax.sql.DataSource;

/**
 * Database configuration for the Ocean View Resort application.
 * Configures the DataSource and connection pool settings.
 */
@Configuration
public class DatabaseConfig {

    // Database configuration is handled via application.properties.
    // Custom DataSource beans can be defined here if needed.
}
